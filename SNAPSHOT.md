# Project snapshot

Initial source snapshot from the uploaded archive is stored in `snapshot/inhPhaton-source.zip`.

The repository is intentionally being staged before functional fixes. The current archive contains the Java sources, configuration, documentation, tests, build metadata, the current JAR, and the task specification.

Known functional defects are documented in the analysis from this conversation and will be addressed in the next changeset.